import React from 'react';

const Home = () => {
  return (
    <div>
      Home Best Component
      <button onClick={() => console.log('Hello!!!')}>Click Me</button>
    </div>
  );
};

export default Home;